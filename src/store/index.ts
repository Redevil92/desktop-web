import { createStore } from "vuex";

import fileSystem from "./fileSystem/fileSystem";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    fileSystem,
  },
});
